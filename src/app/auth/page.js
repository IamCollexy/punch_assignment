'use client';
import LoginForm from '@/components/auth/signIn';
import { Box } from '@mui/material';
import React from 'react';

const page = () => {
  return (
    <Box>
      <LoginForm />
    </Box>
  );
};

export default page;
